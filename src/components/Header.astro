---
import head from '../assets/images/head.jpg';
---

<style>
    header{
      background-size: cover;
      background-position: center;
      height: 100px;
      width: 100%;
      margin-bottom:1rem;
      font-family: var(--font-heading);
      font-size: clamp(1.2rem, 5.8776vw + .2816rem, 3rem);
        /* https://css-clamp-generator.com */
        padding:1rem;
        /* mix-blend-mode: difference; */
        color:#eeeeee;
        text-shadow: #000000 1px 0 5px,#000000 1px 0 15px;
      display:flex;
      align-items: center;
      justify-content: space-between;
      gap:1rem;
    }    
    img{
      height:5rem;
      border-radius:50%;
      border:3px solid #eeeeee;
    }
</style>
<header style={`background-image: url('${head.src}');`}>
  <img src="/andrecollin.jpg" alt=""> Andre Collin
</header>
